language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install

script:
  - hexo clean
  - hexo generate

after_script:
  - cd ./public
  - git init
  - git config user.name "chuxinshuo"
  - git config user.email "kafeiyucha@126.com"
  - git add .
  - git commit -m "Update Blog"
  - git push --force --quiet "https://${CI_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - source
    
env:
 global:
   - GH_REF: github.com/chuxinshuo/chuxinshuo.github.io